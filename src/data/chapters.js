// 教科書コンテンツ (自動生成) - 各種MDファイルから生成されています
export const chapters = [
  {
    "id": 1,
    "title": "Java競プロの基礎と入出力",
    "subtitle": "おまじない・Scanner・出力",
    "content": "# Chapter 1: Java競プロの基礎と入出力\n\nAtCoderなどの競技プログラミングにおいて、Javaで問題を解くための最初のステップを学びます。本章では単なる「おまじない」として済ませず、公式仕様に基づく正確な理解を目指します。\n\n## 1. Javaの基本構造と「おまじない」の意味\n\nJavaのプログラムは必ず**クラス（Class）**の中に書き、その中に**メソッド（Method）**という処理のまとまりを書きます。プログラムを実行したとき、OS（システム）から最初に呼び出されるのが `main` メソッドです。\n\nAtCoderでは、クラス名を必ず `Main` にする必要があります。\n\n```java\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(\"Hello, AtCoder!\");\n    }\n}\n```\n\n### なぜこの書き方になるのか？（公式仕様に基づく解説）\n\n1. **`import java.util.*;`**\n   Javaが標準で提供している便利な道具箱（リストやスキャナーなど）を使うための宣言です。`*`（ワイルドカード）を指定することで、`java.util` パッケージ以下のすべてのクラスを利用可能にします。\n\n2. **`public class Main`**\n   - `public`: どこからでもアクセスできることを示すアクセス修飾子です。\n   - `class`: オブジェクト指向プログラミングの基本単位である「設計図」を定義します。\n\n3. **`public static void main(String[] args)`**\n   - `public`: システム（JVM: Java Virtual Machine）が外部から呼び出すために必要です。\n   - `static`: **クラスをインスタンス化（実体化）しなくても呼び出せる**ようにするためのキーワードです。JVMは `new Main()` を実行せず、直接このメソッドを呼び出します。\n   - `void`: このメソッドが終了した後に、システムに何のデータも返さない（戻り値がない）ことを意味します。\n   - `String[] args`: コマンドライン引数を受け取るための文字列の配列です（競プロでは通常使いません）。\n\n## 2. 標準入力（データの受け取り）とリソース管理\n\nJavaでは入力を受け取る最も簡単な方法が `java.util.Scanner` クラスを使うことです。\n\n```java\nScanner sc = new Scanner(System.in);\n\nint a = sc.nextInt();       // 整数\nlong b = sc.nextLong();     // 大きな整数\nString s = sc.next();       // 文字列 (空白区切り)\ndouble d = sc.nextDouble(); // 小数\n```\n\n### Scannerの内部挙動\n`Scanner` は内部で正規表現エンジンを用いて、空白文字（スペース、タブ、改行など）を区切り文字として認識し、ストリームから型に応じてパース（解析）を行います。便利ですが、解析処理が重いため入力データが数十万件になる場合は後述する「高速入出力」が必要になります。\n\n### リソース管理 (`close()` メソッド)\n本来、標準入力ストリーム（`System.in`）をラップする `Scanner` などのリソースは、使い終わったら `sc.close()` で閉じるのがJavaの公式なベストプラクティスです（メモリやファイルハンドルのリークを防ぐため）。\nただし、**競技プログラミングの環境においては、プログラム自体が数秒で強制終了してメモリがOSに一括返却されるため、`close()` の呼び出しを省略するのが一般的**です。\n\n## 3. 標準出力の詳細\n\n出力を画面（標準出力ストリーム: `System.out`）に書き出す処理です。\n\n```java\nint ans = 42;\nSystem.out.println(\"Ans: \" + ans); // 文字列を結合し、最後に改行して出力\nSystem.out.print(ans);             // 改行なしでそのまま出力\nSystem.out.printf(\"Ans: %03d\\n\", ans); // フォーマット指定出力（042と出力）\n```\n\n- **`println()`** の利点は、OSごとの改行コードの違い（Windowsなら `\\r\\n`、Linuxなら `\\n`）を自動で吸収してくれる点です。\n- **`printf()`** は小数の桁数を指定したい場合などに非常に便利です。（例：`System.out.printf(\"%.5f\\n\", 3.141592);`）\n\n## 4. 競技プログラミングにおけるJavaの注意点\n\n1. **実行時間制限 (TLE: Time Limit Exceeded)**\n   AtCoderの問題には「2秒以内に計算を終わらせる」制限があります。Javaはコンパイル後にJVM上で動作する（バイトコード＋JITコンパイル）ため、起動直後のオーバーヘッドがC++より大きく、実行速度もわずかに劣ります。計算量見積もり（Chapter 9）が不可欠です。\n\n2. **メモリ制限 (MLE: Memory Limit Exceeded)**\n   Javaはガベージコレクション (GC) によって自動でメモリ管理を行いますが、オブジェクトのヘッダ領域などでC++よりもメモリを消費しがちです。巨大な多次元配列などを無駄に生成すると制限に引っかかることがあります。\n"
  },
  {
    "id": 2,
    "title": "変数・データ型・演算子",
    "subtitle": "int / long / オーバーフロー・値渡し",
    "content": "# Chapter 2: 変数・データ型・演算子\n\n**AtCoderで最も多いバグの原因の1つが「型」に関するものです。** Javaの型システムを深く理解し、オーバーフローなどの罠を回避しましょう。\n\n## 1. プリミティブ型と参照型\n\nJavaのデータ型は大きく「**プリミティブ型 (基本データ型)**」と「**参照型**」の2つに分かれます。\n\n### プリミティブ型\nメモリ（スタック領域）に**直接値が保存される**型です。処理が高速で、メモリ効率が良いのが特徴です。\n\n| 型名 | 説明 | メモリサイズ | 範囲 | 競プロでの使い所 |\n|---|---|---|---|---|\n| `int` | 整数 | 32 bit | 約 ±21億 | ループ変数、配列のインデックス、小さな数値 |\n| `long` | 大きな整数 | 64 bit | 約 ±9×10¹⁸ | **少しでも大きくなりそうな数値すべて** |\n| `double` | 浮動小数点数 | 64 bit | 15桁の精度 | 小数の計算（ただし誤差に注意） |\n| `boolean` | 真偽値 | 1 bit相当 | `true` / `false` | フラグ、条件判定 |\n| `char` | 文字 | 16 bit | 1文字 (UTF-16) | 文字列内の1文字の処理 |\n\n### 参照型\nメモリ（ヒープ領域）に確保されたデータの**「住所（参照値）」だけを保存する**型です。配列、文字列(`String`)、クラスなどが該当します。後述する「値渡し」の挙動に大きく関わります。\n\n## 2. 【超重要】オーバーフローと long 型\n\n> **⚠️ AtCoderのD問題以降で最も頻発するバグが「オーバーフロー」です。**\n\nコンピュータは限られたビット数で数値を表現します。`int`型は32ビットなので、扱える最大値（`Integer.MAX_VALUE`）は $2^{31}-1 = 2,147,483,647$（約21億）です。\n\nもし `int` 型の最大値に `1` を足すとどうなるでしょうか？\n**エラーは出ず、符号ビットが反転して約 -21億 にループしてしまいます。** これが「オーバーフロー（桁あふれ）」です。\n\n**【鉄則】** $A \\times B$ が $10^{18}$ になる可能性があるなど、計算途中で少しでも21億を超えそうな変数は、最初からすべて `long` 型で定義して計算する。\n\n```java\nlong a = sc.nextLong();\nlong b = sc.nextLong();\n\n// long同士ならOK\nSystem.out.println(a * b); \n\n// 注意: リテラル（ソースコードに直接書く数値）も、\n// デフォルトではint型と解釈されるため、末尾に 'L' をつけます。\nlong maxLimit = 1000000000000L; \n```\n\n## 3. String クラスの特殊性と「イミュータブル（不変）」\n\nJavaの文字列 `String` は参照型ですが、特殊な性質を持っています。それは**「一度作成された文字列データはメモリ上で絶対に書き換わらない（イミュータブル）」**という点です。\n\n```java\nString s = \"Hello\";\ns = s + \" World\";\n```\n\nこの処理では、`\"Hello\"` という文字列の後ろに `\" World\"` が追加された**わけではありません**。\nメモリ上に全く新しい `\"Hello World\"` というオブジェクトが作られ、`s` という変数の参照先がそこに向き直っただけです。\n（これが後述する Chapter 4 の「文字列結合が遅い理由」に繋がります。）\n\n### 便利なStringメソッド\n```java\nString s = \"Hello\";\nint len = s.length();        // 5 (文字数)\nchar c = s.charAt(0);        // 'H' (0番目の文字を取得)\nString sub = s.substring(1, 4); // \"ell\" (1番目から3番目までの部分文字列を取得)\n```\n\n## 4. 演算子の基本と罠\n\n- **算術演算子**: `+`, `-`, `*`, `/`, `%`\n- **比較演算子**: `==`, `!=`, `>`, `<`, `>=`, `<=`\n\n> **⚠️ 注意事項1：整数の割り算**\n> Javaでは、整数同士の割り算は小数点以下が「切り捨て」られます。\n> `5 / 2` は `2.5` ではなく `2` になります。小数が欲しい場合は `5.0 / 2` のように浮動小数点数を含める必要があります。\n\n> **⚠️ 注意事項2：参照型の同値比較**\n> `String` などの参照型を `==` で比較すると、「同じオブジェクト（同じ住所）かどうか」を判定してしまいます。\n> 文字列の中身が一致しているか判定する場合は、必ず `s1.equals(s2)` を使用してください。\n\n## 5. Mathクラスの活用\n\n計算に便利な `Math` クラスのメソッド（すべて `static` なのでそのまま呼べます）。\n\n```java\nMath.max(a, b);   // 2つのうち大きい方を返す\nMath.min(a, b);   // 2つのうち小さい方を返す\nMath.abs(-15);    // 絶対値 → 15\nMath.pow(2, 3);   // 2の3乗 → 8.0（戻り値が double 型であることに注意）\n```\n"
  },
  {
    "id": 3,
    "title": "制御構文",
    "subtitle": "if / for / while / break・switch式",
    "content": "# Chapter 3: 制御構文\n\nプログラムの流れを制御する「条件分岐」と「繰り返し」について、基礎と競プロで役立つモダンな記法を学びます。\n\n## 1. 条件分岐 (if 文)\n\nある条件が満たされているかどうかで処理を分けます。\n\n```java\nint a = 10;\nif (a > 5) {\n    System.out.println(\"aは5より大きい\");\n} else if (a == 5) {\n    System.out.println(\"aは5です\");\n} else {\n    System.out.println(\"aは5より小さい\");\n}\n```\n\n### 論理演算子の短絡評価（ショートサーキット）\n複数の条件を組み合わせる論理演算子 (`&&` かつ / `||` または) は、**左から右へ評価**され、結果が確定した時点で右側の評価をスキップします。\n\n```java\n// x が 0 の場合、右側の (y / x > 1) は評価されないため、ゼロ除算エラー（ArithmeticException）を防げる\nif (x != 0 && (y / x > 1)) {\n    // 処理\n}\n```\n\n## 2. 【モダンJava】Switch式（アロー構文）\n\nJava 14以降、従来の冗長な `switch` 文が改良され、値を返す「式」として書けるようになりました。競プロでの状態分岐（例えば、方向やコマンドによる処理の分岐）を非常に簡潔に書けます。\n\n```java\nint dir = 1;\n// 従来のように break; を書かなくても自動で抜けるためバグが減る\nString move = switch (dir) {\n    case 1 -> \"Up\";\n    case 2 -> \"Right\";\n    case 3 -> \"Down\";\n    case 4 -> \"Left\";\n    default -> \"Stay\";\n};\n```\n\n## 3. 繰り返し (for / while)\n\n### for文（回数が決まっている反復）\n指定した回数だけ処理を繰り返す場合に使います。\n\n```java\nfor (int i = 0; i < 5; i++) {\n    System.out.print(i + \" \");\n}\n// 出力: 0 1 2 3 4\n```\n> **ポイント**: `i = 0; i < N; i++` の形は、**ちょうど N 回** ループし、配列のインデックス（0から始まる）と完璧に連動するため、競プロの王道パターンです。\n\n### while文（条件が満たされる間の反復）\n回数は未定だが、特定の条件を満たすまで繰り返したい場合（例：二分探索やキューの処理など）に使います。\n\n```java\nint n = 10;\nwhile (n > 0) {\n    System.out.print(n + \" \");\n    n /= 2; // nを2で割り続ける\n}\n// 出力: 10 5 2 1\n```\n\n## 4. ループの制御 (break / continue)\n\n- **`break`**: 現在のループを**完全に終了**し、ループの外へ出ます。\n- **`continue`**: 現在のループの**残りの処理をスキップ**し、次の回の判定（for文なら `i++` の処理）へ進みます。\n\n```java\nfor (int i = 0; i < 5; i++) {\n    if (i == 2) continue; // 2のときは出力せず次へ\n    if (i == 4) break;    // 4のときはループ全体を終了\n    System.out.print(i + \" \"); \n}\n// 出力: 0 1 3\n```\n\n### 【高度】ラベル付き break (多重ループの一括脱出)\n競プロでは、2重ループ（行列の探索など）で目的のものを見つけたら、**2つのループを同時に抜けたい**場面がよくあります。このとき「ラベル」を使うと一発で脱出できます。\n\n```java\nsearchLoop: // ラベル名\nfor (int i = 0; i < H; i++) {\n    for (int j = 0; j < W; j++) {\n        if (grid[i][j] == target) {\n            System.out.println(\"Found at \" + i + \", \" + j);\n            break searchLoop; // searchLoop と名付けた外側のfor文ごと一気に抜ける\n        }\n    }\n}\n```\nフラグ変数（`boolean found = false;`）を用意して各ループで判定するよりも、はるかに簡潔に記述できます。\n"
  },
  {
    "id": 4,
    "title": "配列と文字列処理",
    "subtitle": "1次元/2次元配列・StringBuilder",
    "content": "# Chapter 4: 配列と文字列処理\n\n同じ型のデータを大量に扱う「配列」と、プログラミングに欠かせない「文字列」の処理について学びます。\n\n## 1. 1次元配列とメモリ割り当て\n\n配列は「参照型」です。`new` キーワードを使って、メモリ（ヒープ領域）に領域を確保します。\n\n```java\nint n = sc.nextInt();\n// 要素数 n の配列をメモリ上に確保\n// 基本型（int等）の配列は、自動的に 0 で初期化されます。\nint[] a = new int[n]; \n\nfor (int i = 0; i < n; i++) {\n    a[i] = sc.nextInt();\n}\n```\n\n> **注意**: Javaの配列のインデックスは `0` から始まります。サイズが `n` の場合、アクセスできるのは `0` から `n - 1` までです。`a[n]` にアクセスすると `ArrayIndexOutOfBoundsException` というエラー（RE: Runtime Error）になります。\n\n## 2. 2次元配列（配列の配列）\n\nグリッド（盤面）や迷路を扱う問題で頻出します。Javaの2次元配列は、正確には**「配列を格納した配列」**です。\n\n```java\nint h = sc.nextInt(); // 高さ\nint w = sc.nextInt(); // 幅\nint[][] grid = new int[h][w];\n\nfor (int i = 0; i < h; i++) {\n    for (int j = 0; j < w; j++) {\n        grid[i][j] = sc.nextInt();\n    }\n}\n```\n\n### 【頻出テンプレート】上下左右への移動\n迷路の探索などで、現在の座標 $(y, x)$ から上下左右に移動するための王道パターンです。\n\n```java\n// 右, 下, 左, 上 の順（時計回り）\nint[] dx = {1, 0, -1, 0};\nint[] dy = {0, 1, 0, -1};\n\nint y = 2, x = 2; // 現在地\nfor (int i = 0; i < 4; i++) {\n    int ny = y + dy[i]; // 次のy座標\n    int nx = x + dx[i]; // 次のx座標\n    \n    // 盤面の外に出ないか（境界チェック）\n    if (ny >= 0 && ny < h && nx >= 0 && nx < w) {\n        // 移動可能なら処理を行う\n        // System.out.println(grid[ny][nx]);\n    }\n}\n```\n\n## 3. 文字列処理と StringBuilder\n\n文字列 `String` の文字を1つずつ処理する方法です。文字列は配列とは異なり、`s[i]` のようなアクセスはできません。\n\n```java\nString s = sc.next();\nfor (int i = 0; i < s.length(); i++) {\n    char c = s.charAt(i); // i番目の文字を取得\n}\n```\n\n### 【超重要】文字列結合の罠と StringBuilder\n\n> **⚠️ AtCoderで非常によくあるTLEの原因が「ループ内での文字列結合」です。**\n\nChapter 2で触れたように、Javaの `String` は不変（イミュータブル）です。\n以下のコードを見てください。\n\n```java\n// ❌ 悪い例（TLEの原因）\nString s = \"\";\nfor (int i = 0; i < 100000; i++) {\n    s += \"a\"; \n}\n```\n\nこのコードを実行すると、ループの度に**「古い文字列をコピーし、\"a\" をくっつけた新しい文字列をメモリ上に作り直す」**という処理が発生します。結果として計算量が $O(N^2)$ となり、10万回のループでも数秒かかり TLE になります。\n\nこれを解決するのが **`StringBuilder`** です。これは「変更可能な文字列バッファ（内部的には文字の可変長配列）」であり、$O(1)$ で末尾に文字を追加できます。\n\n```java\n// ⭕ 良い例（O(N) で非常に高速）\nStringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 100000; i++) {\n    sb.append(\"a\");\n}\nString ans = sb.toString(); // 最後に一回だけStringに変換\n```\n\n> **参考**: 似たクラスに `StringBuffer` がありますが、これはマルチスレッド（並行処理）用の安全なロック機構（同期化）がついており、少し動作が遅いです。競プロではシングルスレッドの `StringBuilder` が正解です。\n"
  },
  {
    "id": 5,
    "title": "メソッドとスコープ",
    "subtitle": "関数定義・拡張for文・参照渡し",
    "content": "# Chapter 5: メソッドとスコープ\n\n処理を部品化するための「メソッド（関数）」と、変数の有効範囲（スコープ）、そしてJava特有の非常に重要な罠である「値渡し」について解説します。\n\n## 1. メソッドの定義と呼び出し\n\n同じ処理を何度も書くのを防ぐため、処理のまとまりに名前をつけて再利用できるようにしたものがメソッドです。\n\n```java\npublic class Main {\n    public static void main(String[] args) {\n        int result = add(10, 20); // 呼び出し\n        System.out.println(result); // 30\n    }\n    \n    // 引数（受け取る値）として int を2つ取り、int を返すメソッド\n    public static int add(int x, int y) {\n        return x + y;\n    }\n}\n```\n\n- **`static`**: `main` メソッドが `static` であるため、そこから直接呼び出すメソッドにも必ず `static` をつける必要があります。\n- **戻り値の型**: メソッドが返すデータの型を指定します。何も返さない場合は `void` を指定します。\n\n### メソッドのオーバーロード（多重定義）\n引数の「型」や「数」が異なれば、同じ名前のメソッドを複数作ることができます。（例：整数用と小数用の `add` メソッドなど）\n\n## 2. 変数のスコープとクラスフィールド\n\n変数は、宣言されたブロック `{}` の中だけで有効です。これを**スコープ**と呼びます。\n\n### グローバル変数として使う「クラスフィールド」\n競プロ（特にDFSなどの再帰関数）では、配列や変数を複数のメソッドから共有したい場面が多々あります。その場合はメソッドの外（クラス直下）に変数を定義します。\n\n```java\npublic class Main {\n    // クラスフィールド（グローバル変数のように使える）\n    static int n;\n    static int[] a;\n    \n    public static void main(String[] args) {\n        n = sc.nextInt();\n        a = new int[n];\n        dfs(0);\n    }\n    \n    public static void dfs(int v) {\n        // ここから n や a に直接アクセスできる\n        if (v == n) return;\n        System.out.println(a[v]);\n    }\n}\n```\n\n## 3. 【超重要】Javaの「値渡し (Pass by Value)」と参照型\n\nこれは初心者が最もつまずくポイントです。\n**「メソッドに引数として変数を渡したとき、メソッド内でその変数を書き換えたら、元の変数はどうなるか？」**\n\nJavaの仕様では、引数の渡し方は**すべて「値渡し（コピー渡し）」**です。\n\n### プリミティブ型（int, double等）の場合\n値そのものがコピーされて渡されます。メソッド内で変更しても、呼び出し元には**一切影響しません**。\n\n```java\npublic static void main(String[] args) {\n    int x = 10;\n    changeValue(x);\n    System.out.println(x); // 10 のまま！\n}\n\npublic static void changeValue(int v) {\n    v = 99; // コピーされた v を書き換えただけ\n}\n```\n\n### 参照型（配列やクラス）の場合\n配列の中身そのものではなく、**「配列のメモリアドレス（住所）」の値がコピー**されて渡されます。\nつまり、呼び出し元とメソッド内で、**同じメモリ上の配列を参照**することになります。したがって、メソッド内で配列の要素を変更すると、**元の配列も変更されます**。\n\n```java\npublic static void main(String[] args) {\n    int[] arr = {1, 2, 3};\n    changeArray(arr);\n    System.out.println(arr[0]); // 99 に変わっている！\n}\n\npublic static void changeArray(int[] a) {\n    a[0] = 99; // 渡された住所にある中身を直接書き換えた\n}\n```\n\n**【まとめ】**\n- `int` などの基本型を渡す → **元の変数は変わらない**\n- 配列などを渡す → **配列の中身をいじると元の配列も変わる**\n\nこれを理解しておかないと、DFSや順列生成などのアルゴリズムで致命的なバグを生むことになります。\n\n## 4. 拡張 for 文（for-each 文）\n\n配列やリストの中身を「最初から最後まで順番に見るだけ」の場合、非常に簡潔に書ける構文です。\n\n```java\nint[] arr = {10, 20, 30};\n// 「arrの中身を1つずつ取り出し、xに代入して繰り返す」\nfor (int x : arr) {\n    System.out.println(x);\n}\n```\nただし、中身を上書きしたい場合（`arr[i] = 50;`など）や、インデックス番号 `i` そのものが必要な場合は、通常のfor文を使う必要があります。\n"
  },
  {
    "id": 6,
    "title": "コレクションフレームワーク",
    "subtitle": "ArrayList / Set / Map / Queue / PQ",
    "content": "# Chapter 6: コレクションフレームワーク\n\n「コレクション」とは、データを効率よく格納・検索・操作するためのデータ構造の集まりです。AtCoderのD問題突破には、これらの使い分けが必須条件となります。\n\n## 1. インターフェースと実装クラス\n\nJavaのコレクションは、大きく「どう振る舞うか（インターフェース）」と「どう実装されているか（実装クラス）」に分かれています。\n\n- **インターフェース**: `List` (順序あり), `Set` (重複なし), `Map` (キーと値のペア), `Queue` (待ち行列)\n- **実装クラス**: `ArrayList`, `HashSet`, `HashMap`, `ArrayDeque` など\n\n変数を宣言する際は、左辺をインターフェース、右辺を実装クラスにするのが一般的なお作法です。\n\n```java\n// Listインターフェース型の変数に、ArrayListのインスタンスを代入\nList<Integer> list = new ArrayList<>();\n```\n\n> **注意**: コレクションの中にはプリミティブ型（`int`, `long`, `double`等）を直接入れることはできません。必ずラッパークラス（`Integer`, `Long`, `Double`, `Character`等）を指定します。\n\n## 2. List (可変長配列): ArrayList\n\nサイズが後から自由に変えられる配列です。\n\n```java\nList<Integer> list = new ArrayList<>();\nlist.add(10);           // 末尾に追加: O(1)\nlist.add(20);\nint x = list.get(0);    // インデックスによる取得: O(1)\nlist.set(0, 50);        // 更新: O(1)\nlist.size();            // サイズの取得\n```\n> **計算量の注意**: `list.add(0, 100)` のように「先頭への挿入」や「途中からの削除」を行うと、後ろの要素を全てズラす必要があるため $O(N)$ かかります。\n\n## 3. Set (集合): HashSet / TreeSet\n\n順序を持たず、**重複したデータを許さない**データ構造です。「ある値が存在するかどうか」を高速に判定できます。\n\n```java\nSet<Integer> set = new HashSet<>();\nset.add(10);\nset.add(10); // 既に存在するので無視される\n\n// 含まれているかどうかの判定（非常に高速！）\nif (set.contains(10)) {\n    System.out.println(\"含まれています\");\n}\n```\n\n- **`HashSet`**: ハッシュテーブルを使って実装されており、追加・検索・削除が平均 **$O(1)$** と爆速です。順序はバラバラになります。\n- **`TreeSet`**: 二分探索木で実装されており、処理は **$O(\\log N)$** かかりますが、**常に中身が昇順にソートされた状態**に保たれます。「最小値/最大値を常に取り出したい」場合に強力です。\n\n## 4. Map (連想配列/辞書): HashMap / TreeMap\n\n「キー」と「値」をペアで保存します。競プロでは**「要素の出現回数をカウントする」**目的で非常によく使われます。\n\n```java\nMap<String, Integer> countMap = new HashMap<>();\ncountMap.put(\"apple\", 100);  // データの追加 / 上書き\ncountMap.get(\"apple\");       // 100 (取得)\n\n// 【頻出】出現回数のカウント\nString[] words = {\"apple\", \"banana\", \"apple\"};\nfor (String w : words) {\n    // キーが存在しなければ 0 を返し、それに +1 して保存する\n    countMap.put(w, countMap.getOrDefault(w, 0) + 1);\n}\n```\nSetと同様に、速さ重視なら **`HashMap` $O(1)$**、キーでソートされた状態を維持したいなら **`TreeMap` $O(\\log N)$** を使います。\n\n### ハッシュと equals の概念\n`HashSet` や `HashMap` は、内部でオブジェクトの `hashCode()`（背番号のようなもの）を計算して保存場所を決め、衝突した場合は `equals()` で同一性を判定します。自作クラスをキーにする場合はこれらを正しく実装（オーバーライド）する必要があります。\n\n## 5. Queue / Deque (待ち行列)\n\n「先に入れたものから先に出す (FIFO: First In First Out)」データ構造です。幅優先探索 (BFS) で必須になります。\n\n```java\n// LinkedList より配列ベースの ArrayDeque の方がキャッシュ効率が良く高速！\nQueue<Integer> q = new ArrayDeque<>();\nq.add(1);       // 末尾に追加\nq.add(2);\nint first = q.poll(); // 先頭を取り出しつつ削除 (1が返る)\nint next = q.peek();  // 先頭を見るだけ (2が返る)\nboolean empty = q.isEmpty(); // 空かどうかの判定\n```\n公式ドキュメントでも `LinkedList` より `ArrayDeque` の使用が推奨されています。\n\n## 6. PriorityQueue (優先度付きキュー)\n\n追加した順序に関係なく、**「常に最小値（または最大値）から取り出せる」**特殊なキューです。ダイクストラ法などで必須です。\n\n```java\n// デフォルトは昇順（最小値が先頭）\nPriorityQueue<Integer> pq = new PriorityQueue<>();\npq.add(30); \npq.add(10); \npq.add(20);\nSystem.out.println(pq.poll()); // 10 が出力される\n\n// 降順（最大値が先頭）にしたい場合\nPriorityQueue<Integer> pqMax = new PriorityQueue<>(Collections.reverseOrder());\n```\n内部的には「ヒープ」という木構造で管理されており、追加・取り出しの計算量は **$O(\\log N)$** です。\n"
  },
  {
    "id": 7,
    "title": "ソートとカスタム比較",
    "subtitle": "Arrays.sort / ラムダ式 / Comparable",
    "content": "# Chapter 7: ソートとカスタム比較\n\nデータの並べ替え（ソート）は、アルゴリズムの基本です。Java標準のソート機能と、問題の条件に合わせた独自の並べ替えルールの作り方を学びます。\n\n## 1. 基本的なソート\n\nJavaには、配列用とリスト用にそれぞれソート用メソッドが用意されています。\n\n```java\n// 1. 配列 (int[], long[] など) のソート\nint[] arr = {5, 2, 8, 1, 9};\nArrays.sort(arr); // {1, 2, 5, 8, 9} になる\n\n// 2. リスト (ArrayList など) のソート\nList<Integer> list = new ArrayList<>(Arrays.asList(5, 2, 8));\nCollections.sort(list);\n\n// 3. 降順（大きい順）ソート\n// 注意: プリミティブ型配列 (int[]) には Collections.reverseOrder() は使えません。\n// 一度 Integer[] に変換するか、Listにしてからソートする必要があります。\nCollections.sort(list, Collections.reverseOrder());\n```\n\n### 【Javaの内部仕様】ソートアルゴリズムの違い\nJavaは対象データの型によってソートアルゴリズムを使い分けています。\n- **基本データ型（`int`など）の配列**: `Dual-Pivot Quicksort` が使われます。非常に高速ですが、安定ソート（同じ値を持つ要素の元の順序が保たれる）ではありません。\n- **オブジェクト型（`Integer`や自作クラス）の配列・リスト**: `TimSort`（マージソートの改良版）が使われます。こちらは安定ソートです。\n\n## 2. カスタムソート (自作ルールでの並べ替え)\n\n「文字列の長さ順に並べたい」「クラスの特定のフィールドで並べたい」といった場合、比較ルール（比較器）を自分で定義する必要があります。\n方法は2種類あります。\n\n### 方法A: クラス自体に「自然な順序」を定義する (Comparable)\nオブジェクト自身が「自分は他のオブジェクトより大きいか小さいか」を判断できるようにするインターフェースです。\n\n```java\nclass Item implements Comparable<Item> {\n    int weight, value;\n    \n    // 戻り値の意味:\n    // 負の数: 自分が o より前になる (自分が小さい)\n    // 0: 等しい\n    // 正の数: 自分が o より後ろになる (自分が大きい)\n    @Override\n    public int compareTo(Item o) {\n        // 価値(value)が高い順（降順）にしたい場合\n        if (this.value != o.value) {\n            return Integer.compare(o.value, this.value); \n        }\n        // 価値が同じなら重さ(weight)が軽い順（昇順）\n        return Integer.compare(this.weight, o.weight);\n    }\n}\n\n// 使用例\nItem[] items = new Item[N];\nArrays.sort(items); // compareToのルールに従って自動ソートされる\n```\n\n### 方法B: ソート時にその場限りのルールを渡す (ラムダ式・Comparator)\nわざわざクラスに `Comparable` を実装しなくても、Java 8から導入された「ラムダ式」を使えば、ソートメソッドの引数に直接ルールを渡せます。2次元配列のソートなどで非常に便利です。\n\n```java\nint[][] pairs = new int[N][2];\n// pairs[i][0] は始点、pairs[i][1] は終点とします。\n\n// 始点(インデックス0)の昇順にソートする\nArrays.sort(pairs, (a, b) -> Integer.compare(a[0], b[0]));\n```\n\n> **⚠️【超重要】 引き算による比較の罠**\n> 昔のJavaコードでは `(a, b) -> a[0] - b[0]` と書くテクニックが流行りましたが、**絶対に避けてください**。もし `a[0]` が正の大きな数で `b[0]` が負の大きな数の場合、引き算の結果が `int` の上限を超えて**オーバーフロー**し、大小関係が逆転するバグが発生します。必ず `Integer.compare(a, b)` や `Long.compare(a, b)` を使ってください。\n"
  },
  {
    "id": 8,
    "title": "D問題頻出のアルゴリズム",
    "subtitle": "累積和・二分探索・DFS/BFS・Union-Find・ダイクストラ",
    "content": "# Chapter 8: D問題頻出のアルゴリズム\n\nAtCoderのD問題突破に必須となる、基礎アルゴリズムの考え方とJavaでの実装テンプレートを紹介します。\n\n## 1. 累積和 (Prefix Sum)\n\n配列の一部区間 `[L, R]` の合計を何度も求めたい場合、毎回ループを回すと $O(N)$ かかりますが、累積和を事前に計算しておけば $O(1)$ で求められます。\n\n```java\n// a は元の配列 (1-indexedにしておくと実装が楽)\nlong[] sum = new long[N + 1];\n\n// sum[i] は「a[1] から a[i] までの合計」\nfor (int i = 1; i <= N; i++) {\n    sum[i] = sum[i - 1] + a[i];\n}\n\n// 区間 [L, R] の合計を O(1) で求める\nlong ans = sum[R] - sum[L - 1];\n```\n\n## 2. めぐる式二分探索 (Binary Search)\n\n「ある条件を満たす境界値」を高速（$O(\\log N)$）に見つける汎用的なテンプレートです。「これ以上ならOK、これ未満ならNG」という単調性がある問題で使えます。\n\n```java\nlong ok = 0;             // 確実に条件を満たす値\nlong ng = 1000000000000L; // 確実に条件を満たさない値\n\nwhile (Math.abs(ok - ng) > 1) {\n    long mid = (ok + ng) / 2;\n    if (isOk(mid)) {\n        ok = mid;\n    } else {\n        ng = mid;\n    }\n}\nSystem.out.println(ok); // 境界値が出力される\n```\n\n## 3. グラフ探索 (DFS / BFS)\n\nグラフ（頂点と辺）を探索する2大アルゴリズムです。\n隣接リスト表現：`List<Integer>[] graph = new ArrayList[N];` を使って表現します。\n\n### 深さ優先探索 (DFS: Depth First Search)\n「行けるところまで深く進み、行き止まりになったら戻る」手法です。再帰関数（メソッド自身を呼び出す手法）で実装するのが最も簡単です。\n\n```java\nstatic boolean[] visited; // クラスフィールドに定義\n\npublic static void dfs(int v) {\n    visited[v] = true;\n    for (int next : graph[v]) {\n        if (!visited[next]) dfs(next);\n    }\n}\n```\n\n### 幅優先探索 (BFS: Breadth First Search)\n「出発点から近いところから順番に探索する」手法です。Queue（待ち行列）を使い、**辺のコストが全て等しい場合の「最短経路問題」**に威力を発揮します。\n\n```java\nQueue<Integer> q = new ArrayDeque<>();\nint[] dist = new int[N]; // 出発点からの距離\nArrays.fill(dist, -1);   // 未訪問は -1\n\ndist[0] = 0; \nq.add(0);\n\nwhile (!q.isEmpty()) {\n    int v = q.poll(); // キューの先頭を取り出す\n    for (int next : graph[v]) {\n        if (dist[next] == -1) {\n            dist[next] = dist[v] + 1; // 距離を確定してキューへ\n            q.add(next);\n        }\n    }\n}\n```\n\n---\n\n## 4. 【追加】D問題必須の強力なアルゴリズム\n\n### Union-Find (素集合データ構造)\n「複数の要素が同じグループに属しているか？」「要素Aと要素Bを同じグループに結合する」という操作を、ほぼ $O(1)$ で行える非常に強力なデータ構造です。グラフの連結判定問題で必ず出題されます。\n\n```java\nclass UnionFind {\n    int[] parent; // 親の番号。自分が根の場合は -(そのグループの要素数)\n    \n    public UnionFind(int n) {\n        parent = new int[n];\n        Arrays.fill(parent, -1);\n    }\n    \n    // 根を求める（経路圧縮による高速化）\n    public int root(int x) {\n        if (parent[x] < 0) return x;\n        return parent[x] = root(parent[x]);\n    }\n    \n    // xとyのグループを結合する\n    public boolean union(int x, int y) {\n        int rx = root(x);\n        int ry = root(y);\n        if (rx == ry) return false; // 既に同じグループ\n        \n        // 要素数が多い方にくっつける（サイズによる最適化）\n        if (parent[rx] > parent[ry]) { \n            int temp = rx; rx = ry; ry = temp; \n        }\n        parent[rx] += parent[ry];\n        parent[ry] = rx;\n        return true;\n    }\n    \n    // 同じグループか判定\n    public boolean same(int x, int y) {\n        return root(x) == root(y);\n    }\n}\n```\n\n### ダイクストラ法 (Dijkstra)\n**辺のコストがバラバラ（すべて0以上）**の場合の最短経路アルゴリズムです。BFSを `PriorityQueue` に置き換えた構造になります。\n\n```java\n// クラス定義: 頂点番号と、そこまでの暫定距離をペアにする\nclass Node implements Comparable<Node> {\n    int v; long cost;\n    Node(int v, long cost) { this.v = v; this.cost = cost; }\n    public int compareTo(Node o) { return Long.compare(this.cost, o.cost); }\n}\n\n// 初期化と実行\nPriorityQueue<Node> pq = new PriorityQueue<>();\nlong[] dist = new long[N];\nArrays.fill(dist, Long.MAX_VALUE); // 初期値は無限大\n\ndist[0] = 0;\npq.add(new Node(0, 0));\n\nwhile (!pq.isEmpty()) {\n    Node current = pq.poll();\n    int v = current.v;\n    \n    // 古い情報（より短い経路がすでに見つかっている場合）はスキップ\n    if (dist[v] < current.cost) continue;\n    \n    for (Edge e : graph[v]) {\n        if (dist[e.to] > dist[v] + e.weight) {\n            dist[e.to] = dist[v] + e.weight;\n            pq.add(new Node(e.to, dist[e.to]));\n        }\n    }\n}\n```\n"
  },
  {
    "id": 9,
    "title": "高速入出力と計算量",
    "subtitle": "10⁸の法則・PrintWriter・FastScanner",
    "content": "# Chapter 9: 高速入出力と計算量\n\nD問題以上の難易度では、アルゴリズムの正しさに加えて「実行時間（計算量）」がシビアに問われます。この章ではTLE（実行時間制限超過）を防ぐための見積もりと、Java特有の高速化テクニックを学びます。\n\n## 1. 計算量の見積もり（$10^8$ の法則）\n\n「自分が書いたプログラムが制限時間（通常2秒）に間に合うか？」を見積もるための最強の指標です。\n\n> **「1秒間に処理できるループ（基本演算）の回数は、約 $10^8$（1億）回である」**\n\nAtCoderの入力サイズ（最大値）$N$ に対して、どのような計算量（オーダー）のアルゴリズムを選択すべきかの目安は以下の通りです。\n\n| 問題の最大サイズ $N$ | 許容される計算量 | 実行時間の目安 | 解説・アルゴリズムの例 |\n|---|---|---|---|\n| $10^{18}$ | $O(1)$ または $O(\\log N)$ | ✅ 一瞬 | 算数で直接解く、二分探索 |\n| $10^5$ 〜 $10^6$ | $O(N \\log N)$ 以下 | ✅ 0.02秒 | ソート、累積和、優先度付きキュー |\n| $10^5$ | $O(N^2)$ | ❌ 100億回 (TLE) | **絶対に2重ループを書いてはいけない** |\n| $10^3$ | $O(N^2)$ | ✅ 0.001秒 | 2重ループ |\n| $10^2$ | $O(N^3)$ | ✅ 0.001秒 | 3重ループ (ワーシャルフロイドなど) |\n| $20$ | $O(2^N)$ | ✅ 一瞬 | bit全探索 |\n| $10$ | $O(N!)$ | ✅ 一瞬 | 順列全探索 |\n\n## 2. Javaの「Scanner」が遅い理由\n\nChapter 1で紹介した標準の `java.util.Scanner` は使い勝手が良いですが、内部で正規表現を使った複雑なパース処理を行っています。そのため、入力量が $10^5$ 件（10万件）を超えると、入力の読み込みだけで1秒以上かかり、TLEの原因になります。\n\n## 3. 高速出力: PrintWriter\n\n出力件数が数万件に及ぶ場合、毎回 `System.out.println()` を呼ぶのは致命的に遅くなります。これは毎回の出力処理にOSへのシステムコール（I/O待ち）が発生するからです。\n\n`PrintWriter` は、出力をメモリ上に「バッファリング（溜め込み）」し、ある程度まとまった段階で一気にOSへ送信します。これによりI/O待ちが劇的に減り、数倍〜数十倍高速化されます。\n\n```java\nimport java.io.PrintWriter;\n\npublic static void main(String[] args) {\n    // System.out をラップして PrintWriter を作成\n    PrintWriter out = new PrintWriter(System.out);\n    \n    for (int i = 0; i < 100000; i++) {\n        out.println(i);\n    }\n    \n    // 【重要】最後にバッファに溜まった残りのデータを強制的に吐き出す\n    out.flush(); \n}\n```\n\n## 4. 【コピペ推奨】高速入力: FastScanner\n\n標準の `Scanner` に代わる、バイト単位で直接メモリから読み込んで自力でパース（数値化）するクラスです。\nプロの競プロerは皆、自分用のテンプレートとしてソースコードの下部にこれを貼り付けています。\n\n```java\n// FastScanner テンプレート（D問題以上で推奨）\nimport java.io.IOException;\nimport java.io.InputStream;\nimport java.util.NoSuchElementException;\n\nclass FastScanner {\n    private final InputStream in = System.in;\n    private final byte[] buffer = new byte[1024];\n    private int head = 0;\n    private int tail = 0;\n    \n    private boolean hasNextByte() {\n        if (head < tail) {\n            return true;\n        } else {\n            head = 0;\n            try {\n                tail = in.read(buffer);\n            } catch (IOException e) {\n                e.printStackTrace();\n            }\n            if (tail <= 0) return false;\n        }\n        return true;\n    }\n    \n    private int readByte() {\n        if (hasNextByte()) return buffer[head++];\n        else return -1;\n    }\n    \n    private static boolean isPrintableChar(int c) {\n        return 33 <= c && c <= 126;\n    }\n    \n    public boolean hasNext() {\n        while (hasNextByte() && !isPrintableChar(buffer[head])) head++;\n        return hasNextByte();\n    }\n    \n    public String next() {\n        if (!hasNext()) throw new NoSuchElementException();\n        StringBuilder sb = new StringBuilder();\n        int b = readByte();\n        while (isPrintableChar(b)) {\n            sb.appendCodePoint(b);\n            b = readByte();\n        }\n        return sb.toString();\n    }\n    \n    public long nextLong() {\n        if (!hasNext()) throw new NoSuchElementException();\n        long n = 0;\n        boolean minus = false;\n        int b = readByte();\n        if (b == '-') {\n            minus = true;\n            b = readByte();\n        }\n        if (b < '0' || '9' < b) {\n            throw new NumberFormatException();\n        }\n        while (true) {\n            if ('0' <= b && b <= '9') {\n                n *= 10;\n                n += b - '0';\n            } else if (b == -1 || !isPrintableChar(b)) {\n                return minus ? -n : n;\n            } else {\n                throw new NumberFormatException();\n            }\n            b = readByte();\n        }\n    }\n    \n    public int nextInt() {\n        long nl = nextLong();\n        if (nl < Integer.MIN_VALUE || nl > Integer.MAX_VALUE) throw new NumberFormatException();\n        return (int) nl;\n    }\n    \n    public double nextDouble() {\n        return Double.parseDouble(next());\n    }\n}\n```\n\n使い方は `Scanner` と全く同じです。\n\n```java\npublic static void main(String[] args) {\n    FastScanner sc = new FastScanner();\n    PrintWriter out = new PrintWriter(System.out);\n    \n    int n = sc.nextInt();\n    long x = sc.nextLong();\n    \n    out.println(n + x);\n    out.flush();\n}\n```\n"
  }
];
